name: Build and Deploy

on:
  push:
    branches:
      - dev
  repository_dispatch:
    types: [content-updated]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Start timer
        run: |
          echo "⏰ Build started at: $(date)"
          echo "BUILD_START_TIME=$(date +%s)" >> $GITHUB_ENV
      - name: Log webhook info
        if: github.event_name == 'repository_dispatch'
        run: |
          echo "🚀 Triggered by webhook"
          echo "Event type: ${{ github.event.action }}"
          echo "Client: ${{ github.event.client_payload.userId || 'unknown' }}"
          echo "Reason: ${{ github.event.client_payload.reason || 'unknown' }}"
      - name: Checkout dev branch
        uses: actions/checkout@v4
        with:
          ref: dev
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm' # ← Кэширование node_modules
      
      # Кэширование Eleventy build
      - name: Cache Eleventy build
        uses: actions/cache@v4
        with:
          path: .cache
          key: ${{ runner.os }}-eleventy-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-eleventy-
      
      # Кэширование _site для инкрементальной сборки
      - name: Cache build output
        uses: actions/cache@v4
        with:
          path: _site
          key: ${{ runner.os }}-site-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-site-
          
      - name: Install dependencies
        run: |
          echo "📦 Installing dependencies..."
          npm ci --prefer-offline --no-audit --no-fund
          echo "✅ Dependencies installed"
        
      - name: Build site
        env:
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_BUCKET_NAME: ${{ secrets.R2_BUCKET_NAME }}
          CLIENT_ID: ${{ secrets.CLIENT_ID }}
        run: |
          echo "🔨 Building site for client: $CLIENT_ID"
          # Инкрементальная сборка - пересобираем только измененные файлы
          npx @11ty/eleventy --incremental
          echo "✅ Build completed"
        
      - name: Deploy to main branch
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./_site
          publish_branch: main
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'
          # Оптимизация деплоя
          force_orphan: true
          disable_nojekyll: true
      
      - name: Build completed timer
        run: |
          BUILD_END_TIME=$(date +%s)
          BUILD_DURATION=$((BUILD_END_TIME - BUILD_START_TIME))
          echo "⏰ Build completed at: $(date)"
          echo "🚀 Total build time: ${BUILD_DURATION} seconds"
