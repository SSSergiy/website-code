name: Build and Deploy to Main

on:
  workflow_dispatch: # Ручной запуск из админки через кнопку "Опубликовать"
    inputs:
      clientId:
        description: 'Build only specific client (optional)'
        required: false
        type: string
  push:
    branches:
      - dev # Билдится при push в dev

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout dev branch
        uses: actions/checkout@v4
        with:
          ref: dev
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm' # ← Кэширование node_modules
      
      # Кэширование Eleventy
      - name: Cache Eleventy build
        uses: actions/cache@v4
        with:
          path: .cache
          key: ${{ runner.os }}-eleventy-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-eleventy-
          
      - name: Install dependencies
        run: |
          echo "📦 Installing dependencies..."
          npm ci
          echo "✅ Dependencies installed"
        
      - name: Build sites
        env:
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_BUCKET_NAME: ${{ secrets.R2_BUCKET_NAME }}
          CLIENT_ID: ${{ inputs.clientId }}
        run: |
          if [ -n "$CLIENT_ID" ]; then
            echo "🔨 Building only client: $CLIENT_ID"
          else
            echo "🔨 Building all clients"
          fi
          npm run build
          echo "✅ Build completed"
        
      - name: Deploy to main branch
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./_site
          publish_branch: main
          force_orphan: true
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'
          commit_message: 'Deploy: ${{ github.sha }}'
          keep_files: false
          enable_jekyll: false
